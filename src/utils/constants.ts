/**
 * 枚举类
 */

// ========== COMMON 模块 ==========
// 全局通用状态枚举
export enum CommonStatusEnum {
  ENABLE = 0, // 开启
  DISABLE = 1 // 禁用
}

export enum RequestMethod {
  GET,
  HEAD,
  POST,
  PUT,
  PATCH,
  DELETE,
  OPTIONS,
  TRACE
}
